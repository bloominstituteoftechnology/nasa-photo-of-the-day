import React from"react"

const help = (props) =>{ 
    const {data} = props
    return (
        <div>  
        <p>{data.explanation} hello</p>
        </div>
    )
    }
    
    export default help
import React from 'react'

const Title = ({title}) => {
    return (
        <div 
            style = {
            {
            color: 'white'
        }
    }>
            <h1>{title}</h1>
        </div>
    )
}

export default Title; 
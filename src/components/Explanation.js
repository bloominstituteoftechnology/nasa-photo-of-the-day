import React from 'react'

const Explanation = props => {

    const {explanation} = props

    return (
        <div>
            <p>{explanation}</p>
        </div>
    )
}

export default Explanation
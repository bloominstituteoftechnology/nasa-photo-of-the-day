import React from "react";

const HideSpace = (props) => {
    const {hideImage} = props;

    return (
        <button onClick={hideImage}>Space is scary</button>
    )
}





export default HideSpace;
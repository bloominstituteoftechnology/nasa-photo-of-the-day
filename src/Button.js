// import React from 'react';
// import styled from 'styled-components';
// import theme from "./theme"

// export const Button = styled.button`
// padding: 6px 10px;
// margin: 5px;
// border: none;
// border-radius: 3px;
// color: white;

// &:hover {
//     background-color: white;
//     color: black;
//     text-transform: uppercase;
// }

// ${props => (props.type === 'primary') ? 'background: #2196F3' : null} // Render Prop
// ${props => (props.type === 'success') ? 'background: green' : null}
// ${props => (props.type === 'danger') ? 'background: red' : null}
// ${props => (props.type === 'warning') ? 'background: orange' : null}

// `;
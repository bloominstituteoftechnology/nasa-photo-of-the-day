import React from 'react'

const help = (props) => {

    const {help} = props
    // console.log(data)
    return (
            <img src={help.hdurl} alt=''></img>
        )
    
}

export default help